{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm node scripts/build.js",
    "plugin:build": "pnpm run --filter=vue-middleware build",
    "plugin:build-dts": "pnpm run --filter=vue-middleware build:dts",
    "docs:dev": "pnpm run --filter=docs docs:dev",
    "playground:dev": "pnpm run --filter=playground dev"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.3",
    "@types/node": "^24.10",
    "chalk": "^5.4",
    "fs-extra": "^11.3",
    "node": "^24.12",
    "rimraf": "^6.0",
    "rollup": "^4.40",
    "rollup-plugin-dts": "^6.2",
    "tslib": "^2.8",
    "typescript": "^5.8"
  },
  "dependencies": {
    "vue": "^3.5",
    "vue-router": "^4.6"
  }
}